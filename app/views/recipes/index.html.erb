<h1>RecipeFinder</h1>
<p>Powered By Food2Fork.com</p>

<% if @recipes.nil? %>

<p>Sorry, <i>RecipeFinder</i> can't find recipe for <%= @search_term %> </p>

<% else %>

<p>Found <%= @recipes.count %> recipe(s) for <i><%= @search_term %></i> </p>
<table border="1">
  <tr>
    <th>Thumbnail</th>
    <th>Title</th>
    <th>Social Rank</th>
  </tr>

  <% @recipes.each do |recipe| %>
  <tr class=<%= cycle('even', 'odd') %> >
    <td><%= link_to image_tag(recipe["image_url"], height: '100', width: '100'),
	    recipe["source_url"] %></td>
    <td><%= link_to sanitize(recipe["title"]),
	    recipe["source_url"] %></td>
    <td><%= recipe["social_rank"] %></td>
  </tr>
  <% end %>
</table>
<% end %>
